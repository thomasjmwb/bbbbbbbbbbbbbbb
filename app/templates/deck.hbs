{{#bs-modal name="deckExport" fade=true title="Deck Export"}}
  <form class="form-horizontal">
    <div class="form-group">
      <label class="col-sm-3">Deck contents</label>
      <div class="col-sm-9">
        {{textarea value=exportFormat rows=numberOfUniqueCardsInDeck}}
      </div>
    </div>
  </form>
{{/bs-modal}}
{{outlet}}